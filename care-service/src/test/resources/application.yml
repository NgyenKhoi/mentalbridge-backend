spring:
  application:
    name: care-service
  cloud:
    discovery:
      enabled: false
  liquibase:
    enabled: true
  jpa:
    open-in-view: false

eureka:
  client:
    enabled: false

springdotenv:
  enabled: false
